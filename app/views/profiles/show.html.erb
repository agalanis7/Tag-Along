<%=
  react_component 'Profile',
  props: {
    profile: @profile.attributes.merge( image: url_for(@profile.image)),
    user: @user,
    activities: @activities,
    events: @events }
%>
<%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
